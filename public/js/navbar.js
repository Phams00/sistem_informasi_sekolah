/* ============================================
   NAVBAR.JS
   Interaksi topbar (jika ada yang spesifik)
   Saat ini cukup minimal karena sebagian
   logika sudah di sidebar.js (hamburger toggle)
   ============================================ */

document.addEventListener('DOMContentLoaded', function () {

    // Placeholder: bisa ditambah logika dropdown profil,
    // panel notifikasi, atau search overlay di sini nanti.

});